
describe('Simple test', () => {
    it('Should test that true', () => {
        const result = true
        expect(result).toBe(true)
    })
})

// const jest = require('@jest/globals')
// const BooksController = require('../books.controller')

// describe('Testando lista de livros', () => {
 
// })
